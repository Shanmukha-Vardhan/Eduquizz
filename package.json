{
  "name": "eduquiz-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd src/backend && npm start",
    "install-all": "cd src/frontend && npm install && cd ../../src/backend && npm install",
    "build-frontend": "cd src/frontend && npm run build",
    "heroku-postbuild": "npm run install-all && npm run build-frontend" 
  },
  "engines": {
    "node": "18.x" 
  }
}